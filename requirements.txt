# Core dependencies for TIC parameter recovery simulations
numpy>=1.24
# Optax is used by the GPU-accelerated optimization routines
optax>=0.1.7
# Install JAX; choose the jaxlib wheel matching your platform (CPU/GPU)
jax>=0.4.20
jaxlib>=0.4.20
